export default class ZapierApi {
    /**
     * optOut
     *
     * Send a request to zapier to opt out
     * Works only for Rungwe atm
     *
     *
     */
    static optOut(mobile: string): Promise<void>;
}
